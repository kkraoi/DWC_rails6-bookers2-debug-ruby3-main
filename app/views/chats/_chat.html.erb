<%# 自分が送るメッセージ 右よせ %>
<div class="right-container d-flex justify-content-end" id="chat_<%= chat.id %>">
  <p style="background-color: rgba(0, 185, 0, 0.6); padding: 5px; border-radius: 10px; "><%= chat.message %></p>
  <%= button_to "削除", chat_path(chat), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "btn btn-danger btn-sm ml-2", remote: true %>
</div>